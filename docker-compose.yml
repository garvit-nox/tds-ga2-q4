version: "3.9"
services:
  redis:
    image: redis:7-alpine
    restart: always

  api:
    build: ./api
    restart: always
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
